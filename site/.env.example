# Required — same Postgres database as the pipeline's DATABASE_URL, but WITHOUT
# the "+psycopg" suffix (that's SQLAlchemy-specific; the postgres npm package
# wants a plain postgresql:// URL). Get the base string from Supabase's
# dashboard -> Project Settings -> Database -> Connection string -> URI.
DATABASE_URL=
